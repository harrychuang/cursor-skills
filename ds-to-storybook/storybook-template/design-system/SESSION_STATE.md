# Session State

## Latest Extraction

- Date: 2026-06-17
- Source: Figma node `51090:288762` for Similar Stock Card / `相似股`
- URLs:
  - https://www.figma.com/design/vSr4NtEwPVs6wLpqCT5PtV/%E7%B1%8C%E7%A2%BC-K-%E7%B7%9A-ChipK--Design-System-Lab-?node-id=51090-288762&t=GjcgFzQh0Ll0Qhku-1
- Confidence: High for Similar Stock Card because metadata, screenshot, generated context, and variable definitions were inspected for the concrete node. The visible source confirms a 356x293 `#333333` result card, 332px content width, stock identity, red price/change, orange `相似度 92%` chip, add-to-watchlist action, and a right-side `#252525` feature summary panel with `概念題材`, `公司特徵`, and `市場動態` sections. Down/flat quote, already-added, pressed/focus-visible/disabled/loading, empty/overflow feature text, selected state, and whole-card navigation semantics remain undefined.

## Extraction History

- Figma node `25668:74952`: mobile realtime quote screen, app shell, quote row, top app bar, bottom navigation, floating action pill, edge status toggle.
- Figma node `29209:160554`: portfolio fit chart component set with long-term, swing, short-term, and empty variants.
- Figma node `29207:159069`: portfolio preference header component set with long-term, swing, short-term, and all-operations variants.
- Figma nodes `29208:159790` and `29208:159824`: portfolio fit list header and stock row aligned to the analysis columns.
- Figma node `29209:159854`: portfolio attribute feature labels with lit, unlit, and no-feature variants.
- Figma node `29209:159887`: fit assessment indicators with match and mismatch variants.
- Figma node `29209:162730`: portfolio fit long-term stock bottom sheet with identity, attribute labels, scoring detail table, similar-stock action, and home indicator.
- Figma node `29209:162829`: portfolio bottom-sheet cell variants for stock identity, attribute tabs, and health-check popup title summaries.
- Figma node `29209:162952`: portfolio fit detail table standalone 375x36 header row.
- Figma node `29209:163029`: portfolio fit detail table default and info-row variants.
- Figma node `29210:28828`: standalone similar-stock CTA button.
- Figma nodes `29209:171175`, `29209:171182`, `29209:171392`, and `29209:171407`: preferred-stock title, context top bar, list header, and stock row.
- Figma node `29209:173791`: valuation label variants for expensive, slightly high, slightly low, cheap, and unavailable.
- Figma node `29209:173867`: quarter-line status icon variants with above/below states.
- Figma node `29209:173922`: portfolio health-check stock-list cell variants.
- Figma node `29209:173935`: portfolio health-check full row composition with nine diagnostic columns.
- Figma nodes `29210:26286`, `29210:26310`, and `29210:26525`: portfolio health-check report sections for valuation, quality, and exception scanning.
- Figma node `29199:89866`: portfolio realtime profit/loss summary with collapsed, empty, daily chart, cumulative chart, and allocation chart variants.
- Figma node `29199:89369`: `庫存通用元件` common inventory component frame with Top App Bar, primary tab variants, chevron icon set, and broker import menu.
- Figma node `29199:20518`: primary inventory tab variants for `看盤/盤後`, `即時損益`, `事件/新聞`, and `持股分析`.
- Figma node `29207:96213`: enlarged watchlist realtime quote row with candlestick marker, event metadata, market values, and inline chart.
- Figma node `29207:95624`: main-force trade tag variants for `隔日沖`, `短沖`, and empty state.
- Figma node `29207:95615`: `主力籌碼` stock row with weather signal, volume/rate column, and trade tag.
- Figma node `29207:97253`: main-force weather indicator variants for one-day and three-day weather.
- Figma node `7033:249934`: `庫存股/匯入庫存/券商選單` 42px broker import menu with dropdown, sync/edit actions, and last-update timestamp.
- Figma node `29202:31008`: market filter tab strip variants for `即時報價`, `主力籌碼`, `三大法人`, `當沖`, and `融資券`.
- Figma node `29202:88715`: holdings side action pill variants for `設定`, `編輯`, and `下單`.
- Figma node `29213:87255`: broker/source icon variants for `口袋`, `我的`, `元大`, `國泰`, `新光`, and `大昌`.
- Figma node `23517:229465`: promotional badge variants for AI image import and fee discounts.
- Figma node `8159:289815`: broker import header variants for import broker and usage-state labels.
- Figma node `15937:219728`: broker import row with broker icon, promo badge, helper copy, inline inventory action, and switch slot.
- Figma node `29213:89332`: portfolio add/sync outline action button variants for screenshot sync, manual add, and auto sync.
- Figma node `29215:94535`: portfolio add-holding section variants for dual-button and centered single-action layouts.
- Figma node `7533:257724`: `新增持股` bottom sheet variants for default, duplicate-stock warning, and manual/image tabbed modes.
- Figma node `7533:256932`: `新增欄位/v2` add-holding form body with four 54px rows.
- Figma node `7533:257087`: add-holding `確定` action states with disabled gray outline and default orange fill.
- Figma node `29214:89592`: portfolio manual import value field variants for missing required, entered, and empty values.
- Figma node `29215:89650`: portfolio manual import header and row cell variants for stock identity and value entry.
- Figma node `29215:89662`: portfolio manual import fixed-column row composition.
- Figma node `19215:187463`: relative date label component set with today, future, past, and absolute-only variants.
- Figma node `19215:187525`: event name label component set with dividend, report, meeting, restriction, and capital/share event variants.
- Figma node `19215:187556`: event table row component set with dividend, ex-right, revenue, EPS, meeting, restriction, transfer, and capital increase variants.
- Figma node `19215:187687`: return-today outline button component set with default and disabled variants.
- Figma node `19215:187703`: event filter dropdown trigger component set with all/count variants.
- Figma node `19215:187453`: event filter option component set with default and selected variants.
- Figma node `19215:187371`: major event filter bottom sheet composed from event filter options.
- Figma node `16405:224712`: bottom-sheet header component set with close, back/title/close, title-only, title/close, drag-handle, and no-drag-handle variants.
- Figma node `16405:224726`: bottom-sheet cell component set with icon/text/status rows, action rows, New action row, checked row, switch rows, and main-force period row.
- Figma node `29207:103989`: MyStock 24px utility icons for edit/list/bounce plus the red 42x20 `New` badge asset.
- Figma node `29207:103647`: MyStock 32px utility icon set for sun, sort, inventory, and order actions.
- Figma node `16405:224755`: bottom-sheet composite component set for `設定`, `庫存股_即時損益設定`, `股票排列方式`, `股票排序`, and `加入自選股`.
- Figma node `16405:233530`: standalone bottom-sheet preview selector subcomposition.
- Figma node `16405:224793`: standalone bottom-sheet footer submit button.
- Figma node `8134:289037`: standalone Switch component set with Android and iOS off/on variants.
- Figma node `22911:207990`: standalone Selection Control component set with multi-select checkbox and single-select radio variants.
- Figma node `5906:218640`: centered Popup Dialog component frame with optional title/body/media slots, empty media state, and two-action footer variants.
- Figma node `594:2002`: stock-title Top App Bar variants with short, long, and extreme stock names.
- Figma node `202:4` / child `29503:80044`: Global Bubble anchored callout variants from the broad common-components canvas.
- Figma node `1934:94`: global Button component set with primary fill/outline, five sizes, icon slots, and default/pressed/disabled states.
- Figma node `18095:175915`: Realtime Quote Tile component set with realtime chart/event and after-hours weather variants.
- Figma node `5862:221812`: Empty State component set with optional media, title, and Button action variants around always-present body copy.
- Figma node `51034:5228`: `自選floating btn` attached watchlist Floating Action Pill variants with paired `下單`/`編輯` and edit-only `關閉下單` property.
- Figma node `47327:64981`: `股票標籤` compact stock identity label with code and company name inside a raised dark container.
- Figma node `51036:377157`: `stock-card` default and bullish stock feed/commentary cards with sentiment tag, author metadata, avatar, View More action, and body copy.
- Figma node `51054:298722`: `stock-calendar` return calendar with market-return cells, weekly summary column, and Roboto numeric text.
- Figma node `51059:51226`: Trend Analysis Header / `Frame 480962346` with title actions, gray filled range selection, inactive range options, and `自訂` outline action.
- Figma node `51059:51227`: Asset Trend Chart / `Frame 480961882` with total asset/daily P&L summary, orange area-line chart, muted axis labels, selected-date cursor, and compact date marker.
- Figma node `51059:278139`: Trend Analysis Bar Chart / `Frame 480962365` with signed red/green bars around zero, 37px y-axis label column, 298px plot, and Roboto K-unit axis labels.
- Figma node `46882:50259`: `日曆小格子` Stock Calendar Cell component set with empty, red, green, zero, and no-market variants plus selected-state borders.
- Figma node `51090:288762`: `相似股` Similar Stock Card with stock identity, red quote movement, orange similarity chip, add-to-watchlist action, and right-side feature summary panel.

## Completed Outputs

- Updated token architecture and token files.
- Created evidence map, design principles, design elements, component inventory, interaction states, page composition rules, and anti-AI style rules.
- Extracted component specs for top app bar, bottom navigation, bottom sheet, bottom sheet footer button, bottom sheet header, bottom sheet cell, realtime quote row, realtime quote tile, stock label, stock card, main force stock row, main force weather indicator, main force trade tag, portfolio preference header, portfolio fit chart, portfolio fit list header, portfolio fit stock row, portfolio attribute label, portfolio fit assessment indicator, portfolio stock sheet cell, portfolio fit stock sheet, portfolio fit detail table, similar stock button, similar stock card, portfolio preferred stock title, portfolio preferred stock top bar, portfolio preferred stock list header, portfolio preferred stock row, valuation label, quarter line status icon, portfolio health check cell, portfolio health check row, portfolio health report section, portfolio profit summary, MyStock utility icon, New Badge, broker icon, promotional badge, broker import header, broker import row, broker import menu, portfolio add action button, portfolio add action section, portfolio add holding sheet, portfolio manual import value field, portfolio manual import cell, portfolio manual import row, market tab strip, market filter tab strip, relative date label, event name label, event table row, return today button, event filter dropdown, event filter option, event filter sheet, floating action pill, edge status toggle, switch, selection control, popup dialog, global bubble, button, empty state, stock calendar cell, asset trend chart, and trend analysis bar chart.
- Extracted Popup Dialog tokens and documentation from Figma node `5906:218640`, including `S-080`/`E-080`, `design-system/components/popup-dialog.md`, component-layer popup-dialog tokens, and generic overlay system roles.
- Extracted stock-title Top App Bar variant from Figma node `594:2002`, including `S-081`/`E-081`, Top App Bar component-spec updates, and stock-title component tokens.
- Extracted Global Bubble from Figma child node `29503:80044` under canvas `202:4`, including `S-082`/`E-082`, `design-system/components/global-bubble.md`, callout system roles, and component-layer bubble tokens.
- Extracted Button from Figma node `1934:94`, including `S-083`/`E-083`, `design-system/components/button.md`, explicit primary fill/outline tokens, and action size roles.
- Extracted Realtime Quote Tile from Figma node `18095:175915`, including `S-084`/`E-084`, `design-system/components/realtime-quote-tile.md`, quote-tile size roles, and component-layer tile tokens.
- Extracted Empty State from Figma node `5862:221812`, including `S-085`/`E-085`, `design-system/components/empty-state.md`, Empty State composition tokens, and the decision that its optional CTA composes the existing Button primitive.
- Extended Floating Action Pill from Figma node `51034:5228`, including `S-086`/`E-086`, attached watchlist group tokens, and the decision to keep the paired `下單`/`編輯` control as a Floating Action Pill variant rather than a new Button group.
- Extracted Stock Label from Figma node `47327:64981`, including `S-087`/`E-087`, `design-system/components/stock-label.md`, component-layer stock-label tokens, and the decision to keep it distinct from quote rows, portfolio sheet cells, valuation labels, and main-force trade tags.
- Extracted Stock Card from Figma node `51036:377157`, including `S-088`/`E-088`, `design-system/components/stock-card.md`, feed metadata/sentiment/avatar token roles, component-layer stock-card tokens, and the decision to keep it distinct from quote rows, quote tiles, event rows, dialogs, empty states, bubbles, and broker rows.
- Extracted Stock Calendar from Figma node `51054:298722`, including `S-089`/`E-089`, `design-system/components/stock-calendar.md`, market-return calendar tokens, and the decision to keep it distinct from relative date labels, event rows, return-today controls, and generic calendar-grid tokens.
- Extracted Trend Analysis Header from Figma node `51059:51226`, including `S-090`/`E-090`, `design-system/components/trend-analysis-header.md`, analysis/range system tokens, component-layer trend-analysis header tokens, and the decision to keep it distinct from market tabs, portfolio preference headers, stock calendars, event filter options, and Buttons.
- Extracted Asset Trend Chart from Figma node `51059:51227`, including `S-091`/`E-091`, `design-system/components/asset-trend-chart.md`, chart/asset-trend system tokens, component-layer asset-trend-chart tokens, and the decision to keep it distinct from Trend Analysis Header, portfolio profit summaries, portfolio fit charts, quote mini charts, Global Bubble, and generic chart-card tokens.
- Extracted Trend Analysis Bar Chart from Figma node `51059:278139`, including `S-092`/`E-092`, `design-system/components/trend-analysis-bar-chart.md`, bar-chart system tokens, component-layer trend-analysis-bar-chart tokens, the added green 50% market-down wash, and the decision to keep it distinct from Asset Trend Chart, portfolio profit summaries, quote mini charts, portfolio fit bars, and generic chart-card tokens.
- Extracted Stock Calendar Cell from Figma node `46882:50259`, including `S-093`/`E-093`, `design-system/components/stock-calendar-cell.md`, market-flat system roles, component-layer selected-border tokens, and the decision to keep it as a Stock Calendar subcomponent distinct from full Stock Calendar, Relative Date Label, Event Table Row, Return Today Button, and generic Calendar Grid tokens.
- Extracted Similar Stock Card from Figma node `51090:288762`, including `S-094`/`E-094`, `design-system/components/similar-stock-card.md`, similar-stock result geometry tokens, component-layer card tokens, and the decision to keep it distinct from Similar Stock Button, Stock Card, Realtime Quote Row, Realtime Quote Tile, and Portfolio Fit Stock Sheet.
- Added `design-system/NEAR_TOKEN_REVIEW.md` with near color and near number token candidates.
- Merged the former 16.1px broker icon label primitive into `--cm-ref-size-16` and removed the extra reference token.
- Merged the former 14.063px broker import action label primitive into `--cm-ref-size-14` and removed the extra reference token.
- Merged the former 7.41px portfolio fit chart pointer primitive into `--cm-ref-size-7` and removed the extra reference token.
- Merged the former 2.5px manual import value-field inset primitive into `--cm-ref-size-3` and removed the extra reference token.
- Merged the unused former 3.5px micro-spacing primitive into `--cm-ref-size-4` and removed the extra reference token.
- Merged the deeper dark-shell neutral primitives into `--cm-ref-color-neutral-12` and removed the extra reference tokens.
- Merged the adjacent outline neutral primitives into `--cm-ref-color-neutral-20` and removed the extra reference tokens.
- Merged the neutral control primitive into `--cm-ref-color-neutral-24` and removed the extra reference token.

## Key Decisions

- Use strict `ref -> sys -> comp` token inheritance.
- Preserve mobile-first dark trading-app density.
- Preserve Taiwan market color semantics: red is up, green is down.
- Keep portfolio analysis category colors separate from market movement colors.
- Keep temporal date colors separate from market movement, warning/success, and portfolio category colors.
- Keep event name orange separate from filled primary action semantics.
- Keep event table rows compact, bordered, and data-led when stock identity and event details must be paired.
- Keep return-today and event filter controls compact, outline-based, and 28px high.
- Keep event filter category selection as a compact bottom sheet grid using filled options.
- Keep portfolio preference headers compact and actionable.
- Keep portfolio fit list headers and rows column-aligned with shared size tokens.
- Keep portfolio fit sub-statuses compact and column-bound: 68x20 for fit assessment, 105x24 for attribute labels.
- Keep fit-assessment match/mismatch semantics separate from market movement and generic success/error semantics, even when raw red/green values are reused.
- Keep portfolio fit stock explanation as a compact 375x605 bottom sheet, not a full-screen profile or dashboard.
- Keep portfolio fit detail tables fixed-column and right-align individual performance and score values.
- Keep Portfolio Fit Detail Table header and row variants inside the existing table component; do not create a separate header-row or detail-row component for these nodes.
- Keep Portfolio Fit Detail Table header at 36px and detail rows at 48px with fixed 171/100/64 columns.
- Keep Portfolio Fit Detail Table info rows column-stable when the performance value is empty; preserve the empty 100px value column and the right score column.
- Keep portfolio fit score color separate from market-up semantics even when the raw red value is reused.
- Keep preferred-stock discovery table-led with a 38px title, 44px context top bar, 34px header, and 61px rows.
- Keep preferred-stock list columns independent from the earlier portfolio fit list columns.
- Keep valuation labels as valuation semantics, not market movement or portfolio category semantics.
- Keep portfolio health-check rows diagnostic and horizontally table-led with fixed 659x61 composition.
- Keep health-check stock identity as the only observed sticky-shadow cell.
- Keep quarter-line status red/green semantics tied to the technical condition, not generic success/error.
- Keep portfolio health report detail sections flat, 375px wide, and text/table led; do not convert valuation, quality, or exception details into cards or dashboard widgets.
- Keep portfolio realtime profit/loss as one compact 375px module with a stable three-column summary and optional chart expansion.
- Keep allocation chart colors separate from portfolio attribute category colors.
- Keep secondary market filters compact and text-led: active state is orange label text on the same dark item fill.
- Keep Realtime Quote Row as the owner of the enlarged 79px watchlist/event row; do not fork a separate quote-row component for the candlestick marker variant.
- Keep main-force rows under the `主力籌碼` view as flat 375x61 rows with fixed weather/value/tag slots.
- Keep Main Force Weather Indicator row-bound at 98px wide; do not convert it into a chart, card, or legend.
- Keep Main Force Trade Tag separate from Portfolio Attribute Label, Promotional Badge, New Badge, and Event Name Label because size, semantics, and colors differ.
- Keep main-force red buy signals separate from stock-up market values even when the raw red is reused.
- Keep primary inventory tabs compact and separate from secondary market filters: active state is white label text on the same 37px dark strip, with chevron color only for `看盤/盤後`.
- Keep primary tab system size token names free of component words by using `market-primary` in sys layer and `market-tab-strip` only in component layer.
- Reuse Floating Action Pill for side action sets; `設定`, `編輯`, and `下單` all keep the 68x30 icon-label pill contract.
- Keep broker/source icons container-led and avoid inferring selection, connection, or disabled states from the default icon set.
- Keep promotional badges compact and text-led; do not promote them to CTAs, warning chips, or large marketing tags.
- Keep broker import flows row-led with 37-38px headers and 132px rows; compose rows from Broker Icon, Promotional Badge, and standalone Switch instead of creating card-style account panels.
- Use standalone Switch for broker import switch slots; broker rows own label, icon, promo, helper copy, and placement.
- Keep broker import `查看庫存` as a compact 69x22 outline action, not a filled primary button.
- Keep broker import menus utility-led at the observed/normative 42px height with dropdown, sync/edit outline actions, and timestamp in one row.
- Keep `#909090` as quiet metadata for broker import timestamps rather than reusing `#999999` muted body text.
- Keep portfolio add-holding actions as orange outline buttons inside a flat `#252525` section, not filled CTAs or upload cards.
- Keep screenshot sync, manual add, and auto sync as one add-action button family with variant-specific icon sizing.
- Keep `新增持股` follow-up as a compact 375px bottom sheet with 54px rows and one confirm action; do not convert it into a full-page form or card-based wizard.
- Treat the duplicate-stock red warning as its own add-holding semantic role, not market-up, generic error, or validation banner style.
- Keep the image-recognition tab content undefined until a selected-state reference is supplied; only the inactive tab label is evidenced.
- Keep add-holding form body as an internal 375x216 sheet subcomposition rather than a separate page form.
- Keep add-holding `確定` action scoped to the sheet: disabled is transparent with gray outline/text, enabled/default is orange filled with white label.
- Keep manual import value fields compact at 70x29, with pale-orange outline reserved for missing required values.
- Keep manual import rows fixed-column and horizontally table-like with a sticky 136px stock identity column.
- Keep manual import stock identity cells at 61px row height with compact 18px delete icon, `現股`, stock name, and stock code; do not convert them into form cards.
- Keep relative date labels compact, text-led, and unboxed.
- Keep event name labels text-only and unboxed.
- Keep event table row values right-aligned and preserve muted field labels.
- Keep event filter dropdown trigger separate from the not-yet-observed expanded menu.
- Keep event filter sheet footer actions, backdrop treatment, and scroll behavior undefined until evidenced.
- Keep bottom-sheet header chrome reusable across sheet-specific compositions: close, back, title, drag-handle, and no-handle treatments are slots, not separate page headers.
- Keep bottom-sheet cells row-led and flat on `#252525`; do not convert them into cards or oversized menu panels.
- Keep bottom-sheet action rows as subtle `#3D3D3D` controls with compact 365x62 geometry.
- Use standalone Switch for bottom-sheet switch slots; bottom-sheet cells own row layout, labels, icons, dividers, and right-side placement.
- Use standalone Selection Control for bottom-sheet checkbox/radio slots; bottom-sheet cells and sheets own row labels, placement, and selection grouping.
- Keep the red `New` badge separate from Promotional Badge; it is a compact flat status badge, not a marketing/promo badge.
- Keep MyStock utility icons asset-led; tokens cover size/color and boxed bounce-glyph geometry, not interaction state semantics.
- Keep Bottom Sheet as a composite component above Header and Cell: settings, realtime settings, preview selector, sort selector, and watchlist assignment reuse the same chrome and row contracts.
- Keep display-mode preview selection as two compact `#1E1E1E` thumbnail tiles with orange outline on the selected tile; do not replace it with segmented controls or full-screen previews.
- Keep sort and watchlist selection row fills unchanged; selected state is expressed only through orange check or radio/check controls.
- Keep the orange `完成` footer action scoped to selection-submission sheets such as watchlist assignment; status-only settings sheets do not get a footer CTA.
- Keep the watchlist add sheet internally scrollable with the observed 324px list viewport and 5x215 scroll thumb.
- Extract Bottom Sheet Footer Button as a sheet-level submit subcomponent, not a global button family; disabled/loading/destructive/dual-action states remain undefined.
- Keep Similar Stock Button neutral gray with a search icon and 18px label; do not recolor it as an orange submit CTA.
- Keep Similar Stock Card as a comparison/result card, not a Similar Stock Button CTA, Stock Card feed/commentary card, quote row/tile, or portfolio-fit bottom-sheet section. Preserve the 356x293 card geometry, orange similarity chip, add-to-watchlist action, and right-side feature panel.
- Keep Similar Stock Card observed sizes 356px, 332px, 293px, and 212px as distinct source-backed dimensions because they describe the result-card shell, inner content width, card height, and feature-content width rather than nearby unrelated chart, sheet, popup, or row primitives.
- Keep Portfolio Stock Sheet Cell separate from generic Bottom Sheet Cell because its identity rows, attribute tab rows, and health title summaries are portfolio-domain table structures.
- Keep portfolio stock sheet attribute tabs distinct from standalone Portfolio Attribute Label inactive states: inactive tab labels are muted gray text, not category-color labels.
- Treat quote rows as flat list rows with dividers, not cards.
- Treat portfolio fit empty state as dash-value content, not loading.
- Keep Popup Dialog distinct from Bottom Sheet, Event Filter Sheet, Portfolio Add Holding Sheet, and Bottom Sheet Footer Button because it is centered, 292px wide, has no sheet header/drag/safe-area chrome, and uses media/text slot ordering with a compact two-action footer.
- Normalize observed popup `#414141` secondary/empty artwork strokes to existing `--cm-ref-color-neutral-24` through overlay/control-outline roles instead of adding another near-duplicate dark neutral.
- Keep the 292px popup width as a distinct reference size from nearby 294px and 295px widths because all nine dialog variants share it and those nearby widths already belong to other component-set/action contexts.
- Keep stock-title Top App Bar as a Top App Bar variant instead of a separate component because it shares the dark shell, 44px context rhythm, 27px action boxes, and centered context identity.
- Keep Global Bubble distinct from Popup Dialog, Event Filter Dropdown, Promotional Badge, and chart callouts because it is target-anchored with directional arrows and low elevation.
- Keep Button as the global primary action primitive while preserving Bottom Sheet Footer Button, Portfolio Add Action Button, Similar Stock Button, Return Today Button, and Event Filter Option as scoped components.
- Keep Realtime Quote Tile distinct from Realtime Quote Row and Main Force Stock Row because it owns 170x148 grid-tile geometry and bottom chart/weather content.
- Keep Empty State as an independent page/section feedback component distinct from Popup Dialog, portfolio chart empty value states, and add-action sections. Its optional primary CTA composes the existing Button; Empty State owns only action slot placement and the 266px host width.
- Keep `自選floating btn` as an attached Floating Action Pill variant, not a new Button or separate component; it adds black 152x38 paired quick-action grouping, a 76x38 edit-only mode, divider, and low shadow to the existing floating-action family.
- Keep Stock Label as a display-only compact stock identity component, not a row, valuation badge, trade tag, filter chip, or button. The observed 194px frame is content-derived, so no width token is added.
- Keep Stock Card as a stock-specific feed/commentary card with sentiment and author metadata, not a quote row/tile, event row, popup dialog, empty state, global bubble, or broker import row. Preserve the Figma source spelling `type=defualt` for provenance while exposing a correctly spelled default alias in implementation.
- Keep Stock Calendar as a market-return calendar module, not a date picker, event calendar, or generic calendar grid.
- Keep Stock Calendar Cell as the Stock Calendar's market-day subcomponent. Selected state is border-only; red/green selected cells keep the 30% market wash; the earlier 50% red day remains a high-intensity/highlight state, not selected; zero-cell `#414141` is normalized through the market-flat role and `--cm-ref-color-neutral-24`.
- Keep Trend Analysis Header as a local chart/analysis header with gray range selection and separate `自訂` outline action, not a global tab strip, market filter, event option group, portfolio preference header, stock calendar control, or Button group.
- Keep the observed Trend Analysis Header selected-option shadow normalized to the existing low surface shadow; do not add another near-identical reference shadow.
- Keep Asset Trend Chart as the chart body under trend-analysis controls. It owns the metric summary, orange area-line series, axis labels, selected-date cursor, and date marker; Trend Analysis Header owns title/actions/range filtering.
- Keep Asset Trend Chart distinct from Portfolio Profit Summary, Portfolio Fit Chart, Realtime Quote Tile, Global Bubble, and generic Chart Card tokens.
- Keep the 230px Asset Trend Chart height as distinct chart-body geometry; normalize the observed `#414141` cursor marker fill to neutral-24 and route orange series color through chart-series primary roles.
- Keep Trend Analysis Bar Chart as a distinct signed histogram under trend-analysis contexts. It owns the 375x138 body, 37px y-axis label column, 298px plot, 46px positive/negative bands, 8px square bars, and red/green 50% fills around zero.
- Keep Trend Analysis Bar Chart distinct from Asset Trend Chart, Portfolio Profit Summary, Realtime Quote Tile, Portfolio Fit Chart, and generic Chart Card tokens because it has no summary metrics, orange area-line series, selected-date cursor, quote identity, raised summary surface, or portfolio category table rows.

## Open Questions

- Broker selector expanded menu state is not extracted yet.
- Primary market tab pressed/focus-visible, disabled/unavailable, overflow, long-label wrapping, and expanded `看盤/盤後` menu states need additional Figma references.
- Broker import menu dropdown expanded/selected-broker states, sync/edit pressed/focus/disabled/loading/error states, timestamp absent/format variants, and responsive overflow are not visible in this node.
- Broker icon selected, pressed, focus-visible, disabled, loading, unavailable, connected/disconnected, and account-linking states are not visible in these nodes.
- Promotional badge pressed, focus-visible, disabled, dismissible, loading, long-label, localization, and non-yellow variants are not visible in these nodes.
- Broker import header sticky, sort, pressed/focus-visible, loading, and long-label states are not visible in these nodes.
- Broker import row-specific switch disabled/loading/error handling, row pressed/focus-visible, unsupported broker, no-promo row, long broker names, and sync error states are not visible in these nodes.
- Portfolio add action pressed, focus-visible, disabled, loading, permission, sync-progress, success, and error states are not visible in these nodes.
- Portfolio add action section screenshot sync, permission, syncing, empty-source, loading, and error handling are not visible in these nodes.
- Portfolio add holding selected stock, pressed/focus-visible, loading, image-recognition selected panel, keyboard-open layout, per-field validation, save success, and network/sync error states are not visible in these nodes.
- Portfolio manual import focused editing, caret, keyboard, validation error, disabled, loading, pressed, and hover states are not visible in these nodes.
- Portfolio manual import selected row, row pressed/focus, horizontal scroll runtime, row error summary, successful import, conflict/duplicate state, and disabled states are not visible in these nodes.
- Market filter pressed/focus-visible, disabled/unavailable, horizontal overflow, long-label wrapping, and runtime destination behavior are not visible in these nodes.
- Side action pill pressed/focus-visible, disabled/loading, placement relative to the holdings screen, and collision behavior are not visible in this node.
- Attached Floating Action Pill pressed/focus-visible, disabled/loading, order/edit runtime transition, and hidden-order semantics beyond the observed `關閉下單` edit-only property are not visible in this node.
- Stock Label pressed/focus-visible, selected, disabled, loading, dismissible/removable, icon-leading, and long-name overflow states are not visible in this node.
- Stock Card pressed/focus-visible, selected, bookmarked, loading, empty, collapsed/expanded, bearish, author-overflow, and long-body truncation states are not visible in this node.
- Stock Calendar Cell empty selected, hover, pressed, focus-visible, disabled, loading, error, weekend/holiday, weekly summary-cell extraction, and interaction semantics beyond selected border are not visible in this node.
- Trend Analysis Header other active range states, custom picker open/selected, info tooltip, share pressed/focus-visible, range pressed/focus-visible, disabled, loading, and long-label behavior are not visible in this node.
- Asset Trend Chart loss/neutral P/L, hidden-value, loading, empty, error, crosshair drag/tap, expanded tooltip, focus-visible chart interaction, alternate ranges, and responsive plot resizing are not visible in this node.
- Trend Analysis Bar Chart empty values, loading, error, selected bar, tooltip content, hover, pressed, focus-visible chart interaction, alternate axis scales, x-axis labels, and responsive plot resizing are not visible in this node.
- Loading, disabled, empty, and error market-list states are not visible in this node.
- Main-force stock row pressed/focus-visible, selected, loading, empty, no-weather, no-tag, alternate signal direction, neutral/sell weather colors, and header/sort states are not visible in these nodes.
- Main-force Weather Indicator tooltip behavior, asset taxonomy, accessible icon labels, and weather states beyond one-day `大買` and one three-day sequence are not visible in these nodes.
- Main-force Trade Tag pressed/focus-visible, selected, disabled, loading, localization, long-label, and additional tag categories are not visible in these nodes.
- Portfolio fit chart interactive drilldown and responsive behavior are not visible in this node.
- Portfolio preference expanded menu and action destination states are not visible in this node.
- Portfolio fit list active sort state, info tooltip, row-level mismatch composition, and value overflow states are not visible in these nodes.
- Portfolio attribute label pressed, focus-visible, disabled, hover, other percentage values, overflow, and wrapping states are not visible in these nodes.
- Portfolio fit assessment pressed, focus-visible, disabled, hover, loading, icon-only, and long-label states are not visible in these nodes.
- Portfolio fit stock sheet backdrop, close/back controls, drag handle, scroll behavior, transition, loading, error, and non-long-term variants are not visible in this node.
- Similar Stock Button pressed, focus-visible, disabled, loading, icon-only, and long-label states are not visible in these nodes.
- Similar Stock Card down/flat quote, already-added, pressed, focus-visible, disabled, loading, empty-feature, long-feature overflow, selected, and whole-card navigation states are not visible in this node.
- Portfolio Stock Sheet Cell pressed, focus-visible, disabled, loading, overflow, selected row, alternate stock identity, and alternate health values are not visible in these nodes.
- Portfolio fit detail table score thresholds, tooltip open content, row pressed/focus-visible/disabled/loading behavior, empty/missing value treatment beyond the evidenced info row, row dividers, alternate values, and long-label wrapping are not visible in these nodes.
- Preferred-stock title zero-count, loading, alternate update cadence, and count overflow states are not visible in these nodes.
- Preferred-stock top bar other attribute categories, icon pressed/focus states, and right-action menu behavior are not visible in these nodes.
- Preferred-stock list active sort direction, horizontal overflow, add-to-watchlist selected state, long stock names, loading/empty states, and non-high feature values are not visible in these nodes.
- Valuation label pressed, focus-visible, disabled, loading, selected, and row/list context states are not visible in this node.
- Quarter-line status icon unknown, disabled, loading, and alternate technical-condition states are not visible in these nodes.
- Portfolio health-check cell pressed/focus-visible, selected, loading, empty, disabled, alternate report action, alternate trend direction, and alternate status text states are not visible in these nodes.
- Portfolio health-check row horizontal scroll behavior, sticky-column runtime behavior, responsive clipping, and row selected/pressed states are not visible in these nodes.
- Portfolio health report section empty/no-exception, collapsed, loading, pressed/focus-visible, and long-copy overflow states are not visible in these nodes.
- Portfolio profit summary toggle pressed/focus-visible, loading, error, hidden-cost, long-label overflow, chart tooltip, and detailed chart interaction states are not visible in this node.
- Relative date selected, pressed, focus-visible, disabled, timezone rollover, and localization variants are not visible in this node.
- Event name selected, pressed, focus-visible, disabled, overflow, and category/severity states are not visible in this node.
- Event table row pressed, focus-visible, selected, loading, empty, linked navigation, and complete event taxonomy states are not visible in this node.
- Return-today pressed, focus-visible, hover, loading, and icon states are not visible in this node.
- Event filter dropdown open menu, selected menu item, pressed, focus-visible, disabled, and loading states are not visible in this node.
- Event filter option pressed, focus-visible, disabled, hover, and long-label wrapping states are not visible in this node.
- Event filter sheet close pressed/focus, backdrop, apply/reset, scroll, empty-selection, and maximum selected count rules are not visible in this node.
- Bottom-sheet header close/back pressed/focus-visible states, title overflow, drag-handle interaction, no-title layout, backdrop, and sheet transition rules are not visible in these nodes.
- Bottom-sheet cell pressed/focus-visible, disabled, loading, selected, switch disabled/focus, selection-control disabled/focus, divider variants, long-label wrapping, and right-status overflow states are not visible in these nodes.
- Switch pressed, focus-visible, hover, disabled, loading, indeterminate, text-labeled, reduced-motion, and platform transition states are not visible in this node.
- Selection Control pressed, focus-visible, hover, disabled, loading, error, indeterminate, mixed, text-labeled, and transition states are not visible in this node.
- Bottom-sheet composite backdrop, open/close transition, drag-to-dismiss, keyboard-open layout, destructive action styling, footer disabled/loading states, preview option pressed/focus-visible, and selection-list overflow beyond the observed scroll area are not visible in this node.
- Bottom Sheet Footer Button pressed, focus-visible, disabled, loading, destructive, and dual-action variants are not visible in these nodes.
- Red New Badge long-label, localization, pressed/focus-visible, disabled, dismissible, and non-red variants are not visible in these nodes.
- MyStock utility icon pressed/focus-visible, disabled, selected, loading, color variants, and destination behavior are not visible in these nodes.
- Popup Dialog pressed, focus-visible, disabled, loading, destructive, close-dismiss, single-action, long-copy overflow, backdrop, transition, keyboard-open layout, and responsive placement states are not visible in this node.
- Empty State error, success, compact, body-absent, alternate media asset, replacement illustration, non-primary action, action loading, action disabled, action focus-visible, and long CTA label states are not visible in this node.
- Source copy differs between related components: `短期價差` in the preference header and `短線價差` in the fit chart. Product copy normalization needs user/design decision.
- Desktop/tablet responsive rules are not evidenced yet.

## Token Layers Changed

- `tokens/tokens-ref.css`
- `tokens/tokens-sys.css`
- `tokens/tokens-comp.css`

## Generated HTML Docs

- Generated: `docs/design-system/index.html`
- Generated review queue: `docs/design-system/review.html`

## Audit Result

- 2026-06-17 Similar Stock Card pass: source audit passed. Source inventory rows: 17. Sources with fingerprints: 17. Duplicate source rows: 0. Unresolved source rows: 0. Repeated groups: 0.
- Component audit passed with warnings for 55 older specs missing `Component Fingerprint`. Inventory component rows: 74. Specs: 72. Component similarity rows: 17. Unresolved similarity rows: 0. Source preview visual rows: 17. Missing visual rows: 0.
- `npm run check:tokens` passed.
- Strict token audit still fails because the existing near-token backlog has 324 candidates needing developer decisions: 1 near color pair and 342 near number pairs in the generated review. This pass documented Similar Stock Card near-size keep-distinct decisions for 212px, 293px, 332px, and 356px; the new similar-stock ref sizes did not appear in the unresolved strict token audit output.
- Reference tokens: 318
- System tokens: 820 in strict audit; generated docs rendered 821 system-token rows.
- Component tokens: 2016
- Docs generation: 90 documents and 3155 token rows rendered. The generator still reports missing optional document `INTEGRATION_REVIEW.md`.
- Review queue: 324 candidates plus 17 component similarity rows.

## Source Duplicate Review Result

- No duplicate input source detected for sources `S-081` through `S-094`. `S-081` extends Top App Bar as a stock-title variant; `S-082` is an anchored Global Bubble; `S-083` is the global Button primitive; `S-084` is a grid-style Realtime Quote Tile; `S-085` is a section-level Empty State; `S-086` extends Floating Action Pill with an attached watchlist quick-action variant; `S-087` is a compact display-only Stock Label; `S-088` is a feed-style Stock Card; `S-089` is a market-return Stock Calendar; `S-090` is a local Trend Analysis Header; `S-091` is an Asset Trend Chart body for metric summary and orange area-line visualization; `S-092` is a signed Trend Analysis Bar Chart histogram; `S-093` is a Stock Calendar Cell subcomponent source with day-level market/zero/no-market/selected variants; `S-094` is a Similar Stock Card comparison-result source with stock identity, similarity chip, add-to-watchlist action, and feature summary panel. All are distinct from existing row, sheet, dialog, dropdown, badge, empty-value, domain-action, valuation, trade-tag, tab-strip, calendar, chart, button, feed-card, and quote-tile sources.

## Component Similarity Review Result

- Current similarity queue is fully resolved: 17 rows, 0 unresolved.
- Similar Stock Card compared against Similar Stock Button, Stock Card, Realtime Quote Row, Realtime Quote Tile, and Portfolio Fit Stock Sheet. Developer decision: keep distinct. It owns the 356x293 result-card shell, orange similarity chip, add-to-watchlist action, and right-side feature panel; the compared components own CTA, feed/commentary, quote-list, quote-grid, or portfolio sheet anatomy instead.
- Stock Calendar Cell compared against Stock Calendar, Relative Date Label, Event Table Row, Return Today Button, and generic Calendar Grid tokens. Developer decision: keep distinct as a Stock Calendar subcomponent. It owns the 57x62 day-cell geometry, market-return amount/rate slots, zero/no-market variants, and selected-state border treatment; Stock Calendar owns the surrounding module, header, weekday row, and weekly summary column.
- Trend Analysis Bar Chart compared against Asset Trend Chart, Portfolio Profit Summary, Realtime Quote Tile, Portfolio Fit Chart, and generic Chart Card tokens. Developer decision: keep distinct. It owns the 375x138 signed histogram body, 37px y-axis label column, 298px plot, red/green 50% bars around zero, and no summary/cursor/quote/category-table anatomy.
- Asset Trend Chart compared against Trend Analysis Header, Portfolio Profit Summary, Portfolio Fit Chart, Realtime Quote Tile, Global Bubble, and generic Chart Card tokens. Developer decision: keep distinct. It owns the 375x230 metric-summary chart body, orange area-line series, y/x-axis labels, selected-date cursor, and compact date marker.
- Trend Analysis Header compared against Market Filter Tab Strip, Market Tab Strip, Portfolio Preference Header, Stock Calendar, Event Filter Option, and Button. Developer decision: keep distinct. It owns the local chart-analysis title/action row plus gray range selector and separate `自訂` outline action.
- Stock Calendar compared against Relative Date Label, Event Table Row, Return Today Button, Event Filter Option, and calendar-grid candidates. Developer decision: keep distinct. It owns market-return day cells, weekly summary column, and calendar metrics rather than event/date filtering semantics.
- Stock Card compared against Realtime Quote Row, Realtime Quote Tile, Event Table Row, Popup Dialog, Empty State, Global Bubble, Broker Import Row, and Promotional Badge. Developer decision: keep distinct. It owns feed commentary structure, sentiment tag, avatar, author metadata, and View More affordance.
- Earlier resolved rows remain documented for Switch, Selection Control, Popup Dialog, Top App Bar stock-title variant, Global Bubble, Button, Realtime Quote Tile, Empty State, attached Floating Action Pill, and Stock Label.

## Recommended Next Prompt

Choose the next design-system pass: review/refine the Similar Stock Card extraction; resolve the near-token queue in `design-system/NEAR_TOKEN_REVIEW.md`; generate Figma Variables/token export; create/update cross-agent instructions; or start a Storybook implementation pass with `$design-system-to-storybook`.
