# Design System Kickstart

This workspace is a generic Storybook starter. Populate this folder after running `design-system-extractor` or by following your product design workflow.

## Current Scope

- Placeholder token layers in `tokens/`
- Empty component spec directory in `design-system/components/`
- Storybook addons for Figma export and prototype UI flow review

## Primary Source

- Figma URL: (add your design system file URL here)

## Working Rule

Use tokens first. When a Figma reference introduces a new value, add it to the correct token layer before writing component or implementation guidance.
