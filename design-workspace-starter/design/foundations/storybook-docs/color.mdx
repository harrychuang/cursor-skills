import { Meta, Title, Subtitle, Description } from '@storybook/blocks'

<Meta title="Foundations/Color" parameters={{ layout: 'fullscreen' }} />

<Title>Color Foundation</Title>
<Subtitle>Semantic color roles before component-level exceptions.</Subtitle>
<Description>Use this page to explain what each semantic color role means in practice. Replace the sample labels with the active token names, but keep the decision rules explicit.</Description>

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(12, minmax(0, 1fr))', gap: 16, marginTop: 24 }}>
  <section style={{ gridColumn: 'span 8', padding: 28, borderRadius: 28, background: '#111827', color: '#f9fafb' }}>
    <h2 style={{ marginTop: 0 }}>Semantic Palette</h2>
    <p style={{ marginTop: 0, marginBottom: 16 }}>The first color decision is meaning, not hue. Document which role owns emphasis, containment, neutrality, and feedback.</p>
    <div style={{ display: 'grid', gridTemplateColumns: 'repeat(2, minmax(0, 1fr))', gap: 12 }}>
      <div style={{ padding: 16, borderRadius: 18, background: 'rgba(255,255,255,0.08)' }}>
        <strong>Primary</strong>
        <p style={{ margin: '8px 0 0', opacity: 0.86 }}>Main CTA, selected navigation, or the strongest local emphasis.</p>
      </div>
      <div style={{ padding: 16, borderRadius: 18, background: 'rgba(255,255,255,0.08)' }}>
        <strong>Primary Container</strong>
        <p style={{ margin: '8px 0 0', opacity: 0.86 }}>Large highlighted surfaces that borrow brand emphasis without behaving like a button.</p>
      </div>
      <div style={{ padding: 16, borderRadius: 18, background: 'rgba(255,255,255,0.08)' }}>
        <strong>Surface</strong>
        <p style={{ margin: '8px 0 0', opacity: 0.86 }}>Default canvas, cards, panels, and quiet backgrounds.</p>
      </div>
      <div style={{ padding: 16, borderRadius: 18, background: 'rgba(255,255,255,0.08)' }}>
        <strong>Subtle / Surface Variant</strong>
        <p style={{ margin: '8px 0 0', opacity: 0.86 }}>Secondary regions, neutral chips, and separation without CTA energy.</p>
      </div>
    </div>
  </section>
  <section style={{ gridColumn: 'span 4', padding: 24, borderRadius: 28, background: '#f8fafc', border: '1px solid #cbd5e1' }}>
    <h2 style={{ marginTop: 0 }}>Contrast Rules</h2>
    <p style={{ margin: 0 }}>Call out minimum contrast expectations, inverse text pairings, and the failure cases where color alone is carrying meaning.</p>
  </section>
  <section style={{ gridColumn: 'span 6', padding: 20, borderRadius: 24, background: '#fff7ed', border: '1px solid #fdba74' }}>
    <h3 style={{ marginTop: 0 }}>Feedback Roles</h3>
    <p style={{ marginBottom: 8 }}>Reserve feedback colors for actual state communication:</p>
    <ul style={{ margin: 0, paddingLeft: 18 }}>
      <li>`Error` for validation, destructive flows, and critical alerts</li>
      <li>`Warning` for risk that does not block the flow yet</li>
      <li>`Success` for confirmed completion, not decorative positivity</li>
      <li>`Info` for neutral status and instructional notices</li>
    </ul>
  </section>
  <section style={{ gridColumn: 'span 6', padding: 20, borderRadius: 24, background: '#eff6ff', border: '1px solid #93c5fd' }}>
    <h3 style={{ marginTop: 0 }}>Component Exceptions</h3>
    <p style={{ margin: 0 }}>List component-only tokens only when the semantic layer cannot express the behavior. Document why a badge, chart, or data table needed a local exception.</p>
  </section>
</div>
