# Kickstart

Start from one of these prompts.

## Claude Code

Run `/build`.

## Cursor

Paste:

```text
Read CLAUDE.md, start-here/ACCURACY_CONTRACT.md, start-here/BUILD_PLAN.md, start-here/STORYBOOK_10_AUTODOCS.md, start-here/TASKS.md, product/SCREEN_MANIFEST.json, design/foundations/README.md, skills/design-system-governance/SKILL.md, skills/ui-screenshot-to-storybook-product/SKILL.md, and skills/ui-visual-parity/SKILL.md. Use the latest stable Storybook 10 for shared component work, and make reusable component stories generate Autodocs with component descriptions. Keep Storybook foundations separate from component Autodocs: foundations must be custom MDX front-end pages with designed layouts. Classify the source mode as Figma-first, multi-reference screenshot, or single-image; for image-only work, record observed, inferred, and missing context before implementation. Treat design/foundations as required deliverables: keep color, typography, spacing, corner, design principles, and token usage guidance current, and build or update matching Storybook foundations pages with a design-led bento-style layout when token work changes. Before component implementation, analyze the screenshot and/or Figma source for shared signals in color proportion, spacing feel, corner size, and typography weight. Summarize 5-7 evidence-backed design principles, then define design elements for color, typography, corner, and spacing, clearly separating observed values from inferred system recommendations. Before detailed component docs, define the documentation IA with `Foundations`, `Styles`, and `Components` as the default navigation, then define the standard component page sections: `Overview`, `Anatomy`, `States`, `Usage`, and `Tokens`. Keep each foundation guide concise but deep: recommendation, rationale, usage boundaries, and exceptions. Output the analysis with fixed markdown tables for Signal Summary, Design Principles, Design Elements, and Observed vs Inferred. If .env.local contains FIGMA_FILE_URL and FIGMA_NODE_ID but has neither FIGMA_PAT nor FIGMA_AUTH_MODE=connector, stop and ask the user to set one of them before continuing with Figma-first automation. If .env.local contains the Figma URL and node plus either FIGMA_PAT or FIGMA_AUTH_MODE=connector, run Phase 0 with skills/figma-m3-variables/SKILL.md first, create or audit Ref -> Sys -> Comp variables, bind them to the key source components, and use Figma as source of truth. Otherwise use reference/ screenshots. Do Phase A only after Phase 0 is complete.
```

## Codex

Paste:

```text
Read CLAUDE.md, AGENTS.md, start-here/ACCURACY_CONTRACT.md, start-here/BUILD_PLAN.md, start-here/STORYBOOK_10_AUTODOCS.md, start-here/TASKS.md, product/SCREEN_MANIFEST.json, design/foundations/README.md, skills/design-system-governance/SKILL.md, skills/ui-screenshot-to-storybook-product/SKILL.md, and skills/ui-visual-parity/SKILL.md. Use the latest stable Storybook 10 for shared component work, and make reusable component stories generate Autodocs with component descriptions. Keep Storybook foundations separate from component Autodocs: foundations must be custom MDX front-end pages with designed layouts. Classify the source mode as Figma-first, multi-reference screenshot, or single-image; for image-only work, record observed, inferred, and missing context before implementation. Treat design/foundations as required deliverables: keep color, typography, spacing, corner, design principles, and token usage guidance current, and build or update matching Storybook foundations pages with a design-led bento-style layout when token work changes. Before component implementation, analyze the screenshot and/or Figma source for shared signals in color proportion, spacing feel, corner size, and typography weight. Summarize 5-7 evidence-backed design principles, then define design elements for color, typography, corner, and spacing, clearly separating observed values from inferred system recommendations. Before detailed component docs, define the documentation IA with `Foundations`, `Styles`, and `Components` as the default navigation, then define the standard component page sections: `Overview`, `Anatomy`, `States`, `Usage`, and `Tokens`. Keep each foundation guide concise but deep: recommendation, rationale, usage boundaries, and exceptions. Output the analysis with fixed markdown tables for Signal Summary, Design Principles, Design Elements, and Observed vs Inferred. If .env.local contains FIGMA_FILE_URL and FIGMA_NODE_ID but has neither FIGMA_PAT nor FIGMA_AUTH_MODE=connector, stop and ask the user to set one of them before continuing with Figma-first automation. If .env.local contains the Figma URL and node plus either FIGMA_PAT or FIGMA_AUTH_MODE=connector, read it and run Phase 0 with skills/figma-m3-variables/SKILL.md first, create or audit Ref -> Sys -> Comp variables, and bind them to the key source components before code work. Otherwise use the screenshots under reference/. Build Storybook-first, then compose screens, then run parity comparison and record accepted, deferred, or blocked variance.
```
