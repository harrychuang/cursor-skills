.__FEATURE_CSS_CLASS__ {
  min-block-size: 100vh;
  padding: 24px;
  background: var(--color-background, #f7f7f8);
  color: var(--color-foreground, #111827);
  font-family: inherit;
}

.__FEATURE_CSS_CLASS__--flow-preview {
  min-block-size: 720px;
}

.__FEATURE_CSS_CLASS____header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-block-end: 16px;
}

.__FEATURE_CSS_CLASS____header h1,
.__FEATURE_CSS_CLASS____panel h2,
.__FEATURE_CSS_CLASS____eyebrow {
  margin: 0;
}

.__FEATURE_CSS_CLASS____eyebrow {
  color: var(--color-muted, #6b7280);
  font-size: 12px;
  font-weight: 700;
  text-transform: uppercase;
}

.__FEATURE_CSS_CLASS____nav {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin-block-end: 16px;
}

.__FEATURE_CSS_CLASS____nav button {
  border: 1px solid var(--color-border, #d1d5db);
  border-radius: 8px;
  padding: 8px 12px;
  background: var(--color-surface, #ffffff);
  color: inherit;
}

.__FEATURE_CSS_CLASS____nav button[aria-pressed="true"] {
  border-color: var(--color-accent, #2563eb);
}

.__FEATURE_CSS_CLASS____panel {
  border: 1px solid var(--color-border, #d1d5db);
  border-radius: 8px;
  padding: 20px;
  background: var(--color-surface, #ffffff);
}
