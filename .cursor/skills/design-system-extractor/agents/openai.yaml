interface:
  display_name: "Design System Extractor"
  short_description: "Extract token-backed systems and text components"
  default_prompt: "Use $design-system-extractor to extract a reusable design-system package, including typography tokens and typographic components from screenshots, graphic design references, a Figma URL, or a project folder."

policy:
  allow_implicit_invocation: true
