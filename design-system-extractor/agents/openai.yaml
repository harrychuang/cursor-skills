interface:
  display_name: "Design System Extractor"
  short_description: "Extract and review token-backed design systems"
  default_prompt: "Use $design-system-extractor to extract or integrate a reusable design-system package and static HTML docs from screenshots, a Figma URL, rendered route captures, native iOS/Android app captures, branch diffs, a project folder, or a vibe-coded prototype."

policy:
  allow_implicit_invocation: true
