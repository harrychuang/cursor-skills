## 1. Project Config And Initialization

- [x] 1.1 實作 Generated Project Configuration，並落實 Use a generated project config as the template boundary：新增 .storybook/project.config.ts typed config，讓 projectName、packageName、tokenPrefix、componentClassPrefixes、review、Figma source、storySort、prototype inspector settings 都能從同一份設定讀取；以 npm run typecheck 驗證 config 型別與現有 imports 成立。
- [x] 1.2 實作 Token Prefix Validation，並落實 Initialize by generation, not repository-wide replacement：新增 npm run init-template 入口與 scripts/init-storybook-template.mjs，命令接受 --name、--prefix、--package-name、--figma-url、--keep-example、--force，且 invalid prefix 不寫入檔案；以 node scripts/init-storybook-template.mjs --name "Design Lab" --prefix acme-ds1 和 --prefix Cm_DS 分別驗證成功與失敗路徑。
- [x] 1.3 實作 Project Config As Source Of Truth：將 .storybook/main.ts、.storybook/preview.ts、.storybook/figma-export.config.ts 改為讀取 project config 的 review endpoint、status file、componentClassPrefixes、storyTitlePrefix、absoluteFidelityComponents、Figma fallback URL、node overrides 與 storySort；以 npm run typecheck 和人工檢查 Storybook config exports 驗證不再依賴 hardcoded cm values。

## 2. Config-Driven Checks And Neutral Core

- [x] 2.1 實作 Prefix-Agnostic Token Inheritance Check：scripts/check-token-inheritance.mjs 從 project config 取得 tokenPrefix，依 --<prefix>-ref、--<prefix>-sys、--<prefix>-comp 驗證 ref -> sys -> comp inheritance，錯誤訊息包含檔案、token name、actual layer、expected layer；以 npm run check:tokens 驗證成功路徑，並用一個暫時 cross-layer fixture 驗證失敗訊息後還原 fixture。
- [x] 2.2 落實 Make token and catalog checks config-driven：scripts/check-storybook-catalog.mjs 和 src/storybook/componentCatalog.ts 支援 neutral template catalog，不要求 ChipK categories 或 domain story titles；以 npm run check:storybook-catalog 驗證 neutral component story 與 catalog metadata 對齊。
- [x] 2.3 實作 Template Core Identifier Neutrality：移除 runtime/build/check/prototype inspector 中對 cm、ChipK、inventory-prototype 的必要依賴，保留的 legacy identifiers 必須只存在於 sample/reference 或 migration documentation；以 rg "cm-|--cm|chipk|ChipK|inventory-prototype" .storybook scripts src/storybook src/pages/prototypes src/stories design-system tokens package.json 的結果人工確認命中符合允許範圍。

## 3. Prototype UI Flow Core

- [x] 3.1 實作 Prototype Metadata Contract 和 Prototype Review Modes：prototype inspector 只從 parameters.prototype 讀 id、title、description、docs、flow、data，且 Story mode 保持原 story render，Docs/Data mode 對缺漏資料顯示中性 empty state；以 example prototype 在 Story、Docs、Data 三個 mode 的人工檢查驗證。
- [x] 3.2 實作 UI Flow Canvas，並落實 Keep Prototype UI Flow independent from project prefix and domain：UI Flow mode 從 flow.routes、flow.nodes、flow.transitions 產生 route cards、flow-only nodes 與 flowLine 等於 key 的 visible edges，不依賴 token prefix、component id 或 route visible label；以 example prototype 檢查兩個以上 route cards、至少一個 flow-only node、以及 key edge filtering 行為。
- [x] 3.3 實作 UI Flow Layout Persistence：dragged positions、fit/manual zoom、Export Layout、Import Layout 使用 generic schema、version、prototypeId、exportedAt、positions payload，非 matching layout 匯入時保留目前 layout 並顯示錯誤訊息；以 example prototype 匯出 JSON、重新匯入 JSON、再匯入不匹配 JSON 的人工流程驗證。
- [x] 3.4 實作 Route Preview Measurement：iframe 高度量測優先使用 data-prototype-route-preview="true" marker，沒有 marker 時 fallback 到 Storybook root 或 document body，且不查詢 inventory-prototype selector；以 example prototype route preview 與一個無 marker fallback story 人工檢查 UI Flow card 高度穩定。
- [x] 3.5 實作 Static Flow Export：當 metadata 提供 flow export story id 時 UI Flow 顯示 Open Static Flow control，點擊後開啟 sibling static flow story 並啟用 Figma export globals；當 metadata 缺少 flow export story id 時 control 不顯示；以 example prototype 和一個無 static export metadata 的 story 人工驗證。
- [x] 3.6 實作 Static Flow 與 UI Flow 同步：抽出 generic prototype flow layout helper，讓 UI Flow inspector 與 example Static Flow 使用相同 storage key、schema、position normalization；Static Flow 先讀取 saved UI Flow positions 並監聽 storage 更新，再 fallback 到 metadata positions，且 route preview width/height 與 UI Flow iframe preview contract 一致；以 npm run typecheck、npm run storybook:build、npm run check、spectra validate generalize-storybook-template --strict 驗證。
- [x] 3.7 實作 Static Flow visual parity：讓 example Static Flow 使用與 UI Flow mode 一致的 dotted canvas、route card chrome、flow-node shape rules、edge color variants、arrowheads、label pill conventions，並繼續套用 saved UI Flow positions；以 npm run typecheck、npm run storybook:build、npm run check、spectra validate generalize-storybook-template --strict 驗證。

## 4. Neutral Example And Default Surface

- [x] 4.1 實作 Neutral Example Prototype，並落實 Use a neutral example prototype as a contract test：建立 example-prototype 的 interactive story、static flow export story、flow metadata、fixture data、prototype meta、scoped CSS、PRD、UI_SPEC、FLOW_SPEC、DATA_SPEC、ACCEPTANCE、IMPLEMENTATION_GUIDE，內容使用中性 Project Intake 或 Task Review domain；以 Storybook 中 example prototype 四個 review modes 都有內容驗證。
- [x] 4.2 實作 Default Template Surface，並落實 Move ChipK material out of the default template surface：初始化後預設 Storybook navigation 只載入 generic governance、foundation、starter component、example prototype，不載入 stock、inventory、broker、portfolio domain stories；以 npm run storybook:build 和人工檢查 story index 或 built navigation 驗證 domain stories 不在 default surface。
- [x] 4.3 更新 README 與 design-system guidance：文件說明 init-template 使用方式、prefix 規則、新增 component 流程、新增 prototype 流程、Prototype UI Flow metadata contract、ChipK sample/reference 邊界與驗證命令；以人工內容審查確認每個命令、設定欄位與驗證步驟都能對應到已實作行為。
- [x] 4.4 Remove legacy component source from the template surface：刪除 src/components 中除 example-card 外的 ChipK domain component source，並刪除仍依賴這些元件的 src/pages/inventory-realtime-quote-page 與 src/pages/prototypes/inventory-prototype；更新 prototype README 不再指向 removed inventory reference；以 rg 檢查 src 中不再有 legacy component imports，並以 npm run typecheck、npm run storybook:build、npm run check、spectra validate generalize-storybook-template --strict 驗證 template 不受影響。
- [x] 4.5 Generate the Figma importer plugin from the addon，並落實 Generic Figma Importer Plugin：在 vendored Storybook Figma export addon 中提供 DOM-free plugin-code subpath 與 createFigmaImporterPluginMainCode，新增 scripts/build-figma-plugin.mjs 由 addon 產生 figma/storybook-code-to-design/main.js，讓 plugin 依 payload metadata 匯入 token collections、plugin data keys、component metadata 與 page artifacts 而不是 project prefix/domain；將 check:figma-plugin 納入 npm run check，並以 npm run build:figma-plugin、npm run check:figma-plugin、npm run typecheck、npm run check、npm run storybook:build、spectra validate generalize-storybook-template --strict 驗證。
- [x] 4.6 Fix Figma importer runtime syntax compatibility：移除 addon-generated importer runtime 中的 optional chaining/nullish coalescing/optional catch binding，修正 importer function extraction 遺漏 closing brace 的問題，讓 figma/storybook-code-to-design/main.js 可被 Figma plugin parser 載入；同步將 TRUNCATE text auto-resize 改用 textTruncation fallback，並讓 scripts/build-figma-plugin.mjs --check 在產物含不支援語法或 invalid JavaScript 時失敗；以 npm run build:figma-plugin、npm run check:figma-plugin、npm run check、npm run storybook:build、spectra validate generalize-storybook-template --strict 驗證。

## 5. Final Verification

- [x] 5.1 完成 end-to-end verification：在乾淨初始化結果上執行 npm run check、npm run storybook:build、npm run typecheck，並人工驗證 example prototype 的 Story、Docs、UI Flow、Data、layout import/export、Open Static Flow；所有命令必須通過且人工驗證結果記錄在 closeout summary。
