interface:
  display_name: "Design System to Storybook"
  short_description: "Build framework-native Storybook systems"
  default_prompt: "Use $design-system-to-storybook to select or inherit the correct app framework, renderer, builder, and repo conventions, then build token-backed Storybook components and docs with only compatible optional tooling."

policy:
  allow_implicit_invocation: true
