Read `product/PRD.md`, `product/PRODUCT_SPEC.json`, and `product/FEATURE_MANIFEST.json`.

Normalize product requirements before editing UI code:

1. Update the written PRD.
2. Sync product and feature manifests.
3. Map each feature to affected screens in `product/SCREEN_MANIFEST.json`.

Do not implement large UX changes before the manifests reflect them.
